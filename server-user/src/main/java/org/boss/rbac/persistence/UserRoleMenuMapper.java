package org.boss.rbac.persistence;

import org.apache.ibatis.annotations.Param;
import org.boss.rbac.pojo.dto.UserDTO;

public interface UserRoleMenuMapper {
//    UserDTO getUserRoleMenu(@Param("username") String username, @Param("role") String role);
}
